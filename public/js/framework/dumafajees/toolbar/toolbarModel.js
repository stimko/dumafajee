define(['../base/baseModel', '../buttons/simple/simpleButtonModel'], 
  function(Base, SimpleButtonModel) {
  return Base.extend({
    default:{
      id:'Rhythm.toolbar',
      items: []
    }
  });
});